#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	char tc;

	for (tc = 'A' ; tc <= 'Z' ; tc++)
	{
		putchar(tc);
	}

	return (0);
}
