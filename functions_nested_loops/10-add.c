#include "main.h"

/**
 * add - Write a function that adds two integers and returns the result.
 * @a: check
 * @b: check
 *
 * Return: Always 0 (Success)
 */

int add(int a, int b)
{
	return (a + b);
}
